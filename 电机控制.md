# Emm_V5.0 闭环步进电机控制总结

> 参考: `F:\1A TI library\26电赛\yifan示例代码`
> 目标平台: TI MSPM0G3507 + DriverLib（已由 STM32/FreeRTOS 版本移植）
> 通信方式: UART 串口，波特率 115200，校验字节固定 `0x6B`

## 一、示例代码文件结构

示例目录下有两套代码，功能相同但载体不同：

| 目录 | 载体 | 特点 |
|---|---|---|
| `Emm/` | TI MSPM0 + FreeRTOS | `usart_SendCmd()` 通过 `uart_transfer` + `ulTaskNotifyTake` 阻塞等待发送完成 |
| `STM32_串口通讯__速度模式/` | STM32F1 + 标准外设库 | `usart_SendCmd()` 直接调用 `USART_SendData` 阻塞发送 |

两套代码的 `Emm_V5.c/h` 内容一致，仅底层 `usart_SendCmd` 的实现不同。协议本身与平台无关，只要能把字节数组按顺序送出去即可。

## 二、协议帧格式

所有指令帧的通用结构：

```
[地址] [功能码] [参数...] [校验码 0x6B]
```

- **地址**：1 字节，默认 `0x01`，可用 `0x00` 广播
- **功能码**：1 字节，每条指令固定
- **辅助码**：部分指令有第 2 字节固定"子码"（如速度控制第 1 字节 `0xF6`，无子码；使能第 1 字节 `0xF3`，第 2 字节 `0xAB`）
- **校验码**：本示例采用固定 `0x6B` 校验（驱动器默认设置）

## 三、核心 API 一览

`Emm_V5.h` 暴露的关键函数（示例中）：

| 函数 | 作用 | 关键字节序 |
|---|---|---|
| `Emm_V5_En_Control(addr, state, snF)` | 使能/失能电机 | `addr, 0xF3, 0xAB, state, snF, 0x6B` |
| `Emm_V5_Vel_Control(addr, dir, vel, acc, snF)` | 速度模式 | `addr, 0xF6, dir, velH, velL, acc, snF, 0x6B` |
| `Emm_V5_Pos_Control(addr, dir, vel, acc, clk, raF, snF)` | 位置模式（脉冲数） | `addr, 0xFD, dir, velH, velL, acc, clk[3..0], raF, snF, 0x6B` |
| `Emm_V5_Stop_Now(addr, snF)` | 立即停止 | `addr, 0xFE, 0x98, snF, 0x6B` |
| `Emm_V5_Synchronous_motion(addr)` | 多机同步启动 | `addr, 0xFF, 0x66, 0x6B` |
| `Emm_V5_Reset_CurPos_To_Zero(addr)` | 当前位置清零 | `addr, 0x0A, 0x6D, 0x6B` |
| `Emm_V5_Reset_Clog_Pro(addr)` | 解除堵转保护 | `addr, 0x0E, 0x52, 0x6B` |
| `Emm_V5_Modify_Ctrl_Mode(addr, svF, mode)` | 切换开环/闭环 | `addr, 0x46, 0x69, svF, mode, 0x6B` |
| `Emm_V5_Origin_*` | 一整套回零接口 | 参见头文件 |

### 速度模式参数含义

```c
void Emm_V5_Vel_Control(uint8_t addr, uint8_t dir, uint16_t vel,
                        uint8_t acc, bool snF);
```

- `addr`：电机地址（默认 1）
- `dir`：`0 = CW（正转）`，`非0 = CCW（反转）`
- `vel`：转速 RPM，范围 `0 ~ 5000`（16 位大端拆分）
- `acc`：加速度档位 `0 ~ 255`，`0 = 直接达到目标速度`
- `snF`：多机同步标志，`false = 立即执行`，`true = 缓存等同步指令`

### 使能控制

```c
Emm_V5_En_Control(addr, true, false);   // 使能（锁轴），立即生效
Emm_V5_En_Control(addr, false, false);  // 释放（可手转）
```

**上电后必须先延时 2 s** 等待驱动器闭环编码器初始化完成，再发送指令。

## 四、示例 main 函数流程（STM32 版）

```c
board_init();                       // 时钟/GPIO/USART 初始化
delay_ms(2000);                     // 等驱动器闭环初始化
Emm_V5_Vel_Control(1, 0, 1000, 10, 0);  // 地址1, CW, 1000RPM, acc=10, 立即执行
while (rxFrameFlag == false);       // 等一帧应答
while (1) { }                       // 主循环
```

要点：
1. 上电必须延时 2 s
2. `Emm_V5_Vel_Control` 是单帧发送后立即返回，电机会持续按该速度旋转
3. 想让电机停下必须调用 `Emm_V5_Stop_Now` 或再发一个 `vel=0` 的速度指令

## 五、FreeRTOS 版云台跟踪示例（gimbal.c）要点

- 两台电机（YAW `0x01` / PITCH `0x02`）分别控制水平和俯仰
- 每 20 ms 读一次视觉板给出的目标像素与激光像素，计算偏差 `dx`, `dy`
- **PD 控制**：`speed = KP*|err| + KD*(|err|-|prev_err|)`
- 死区 3 px，最低转速 1 RPM，最高 12 RPM
- 首帧收到目标才使能电机，防止误动作

## 六、本项目移植说明

- `Emm_V5.c/h`：函数签名多了 `UART_Regs *uart` 参数（TI 风格），底层通过 `UART_send_bytes(uart, cmd, sizeof(cmd))` 发送
- `MOTOR_DIR_CW=0`、`MOTOR_ENABLE=1`、`MOTOR_SYNC_NOW=0` 等宏定义在 `Emm_V5.h` 内
- 与电机通信的实例：`PC_INST`（UART0，见 SysConfig）
- `UART_send_bytes` 会同步把每一字节镜像到 `DEBUG_INST`（若定义），便于抓包

## 七、最小控制序列（正转 200 RPM）

```c
SYSCFG_DL_init();                       // 时钟/UART/GPIO 初始化
delay_ms(2000);                         // 等驱动器上电就绪

Emm_V5_En_Control(PC_INST, 1, MOTOR_ENABLE, MOTOR_SYNC_NOW);
delay_ms(100);

Emm_V5_Vel_Control(PC_INST, 1,
                   MOTOR_DIR_CW, 200, 10,
                   MOTOR_SYNC_NOW);
// 之后电机将以 200 RPM 持续正转
```

停止：`Emm_V5_Stop_Now(PC_INST, 1, MOTOR_SYNC_NOW);`
