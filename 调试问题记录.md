# 电机控制调试问题总结

> 项目：26h-tx-rx-motor
> 平台：TI MSPM0G3507 + ZDT_X42S 闭环步进电机（Emm 固件）
> 日期：2026-07-30

---

## 最终根因

**`uart.c` 中 `UART_send_bytes` 的 DEBUG 镜像功能**导致 PA28 和 PA8 两个 GPIO 引脚同时翻转，GPIOA 供电轨抖动，PA28 信号边沿质量下降。TTL 串口模块（带施密特触发器）能正确解码，但电机接收端对信号完整性更敏感，会误读字节 → 电机不转。

### 修复方法

注释掉 `uart.c` 第 22-24 行的 DEBUG 镜像代码：

```c
// #ifdef DEBUG_INST
//         if (uart != DEBUG_INST) {
//             UART_send_char(DEBUG_INST, data[i]);
//         }
// #endif
```

---

## 问题排查全过程

### 问题 1：编译报错 `unresolved symbol: status`

**现象**：链接时报 `undefined symbol: status`，`key.o` 引用但找不到定义。

**原因**：`key.c` 中 `extern volatile int status;` 是外部引用声明。精简 `empty.c` 时误删了 `volatile int status = 0;`。

**解决**：保留 `empty.c` 中的 `volatile int status = 0;` 定义，满足 `key.c` 的外部引用。**变量引用是编译层面硬依赖，任何 `.c` 文件 `extern` 的变量必须有且只有一个文件提供全局定义。**

---

### 问题 2：电机只收到 `FA EB`，没有后续数据

**现象**：把电机 RX/TX 接到 TTL 串口模块，串口助手上只显示 `FA EB` 两个字节，后续无数据。

**原因**：`SYSCFG_DL_PC_init()` 只使能了 UART 外设级中断，**没有调用 `NVIC_EnableIRQ(UART0_INT_IRQn)` 使能 NVIC 级中断**。电机上电后的应答帧堆积在 RX FIFO 中无人读取，FIFO 溢出后 UART 进入异常状态。

**解决**：在 `SYSCFG_DL_init()` 后添加 `NVIC_EnableIRQ(UART0_INT_IRQn);` 使能 NVIC 中断。

**教训**：SysConfig 生成的 `SYSCFG_DL_xxx_init()` 只配外设寄存器，不会自动配 NVIC。**所有外设中断都需要手动调用 `NVIC_EnableIRQ()`。**

---

### 问题 3：指令全部发出但电机不转

**现象**：串口助手监听到 MSPM0 TX 发出了完整指令序列（`01 0E 52 6B`、`01 F3 AB 01 00 6B`、`01 F6 00 00 C8 0A 00 6B`），电机有阻尼（使能成功）但不转。

**原因**：

1. **编码器校准指令 `01 06 45 6B` 干扰**：校准指令发出后电机内部状态机进入"校准中"状态，后续使能+速度指令被忽略。及时电机已校准过不执行物理转动，内部状态切换也需要时间。

2. **不需要的解除保护指令**：`01 0E 52 6B`（解除堵转保护）在电机正常状态下发是多余的。

**解决**：去掉校准指令和解除保护指令，直接从使能开始。**不要发不必要的指令，每条指令都可能影响电机内部状态机。**

---

### 问题 4：RX FIFO 应答帧堆积

**现象**：每条指令发出后，电机会返回 4 字节应答帧（如 `01 F3 02 6B`）。连发 3 条指令 → 12 字节应答堆积在 UART RX FIFO 中。MSPM0 UART RX FIFO 深度只有 4 字节 → 溢出 → UART 进入 OVERRUN 错误状态。

**解决**：每条指令发送并延时后，手动清空 RX FIFO：

```c
while (DL_UART_isRXFIFOEmpty(PC_INST) == false) {
    DL_UART_receiveData(PC_INST);
}
```

**教训**：MSPM0 的 UART FIFO 只有 4 字节深度。**每次发送指令后必须清空 RX FIFO**，否则电机应答帧会堆积溢出。

---

### 问题 5：PA28 vs PA8 — 同样字节不同结果

**现象**：
- 串口助手通过 TTL 发 `01 F6 00 00 C8 0A 00 6B` → 电机 **转了**
- MSPM0 通过 PA28 (UART0) 发同样的字节 → 电机 **不转**
- MSPM0 通过 PA8 (UART1) 发同样的字节 → 电机 **转了**

**排查过程**：

1. **排除 RX 中断干扰**：关闭 `DL_UART_MAIN_INTERRUPT_RX` 后仍不转 → 不是中断问题
2. **排除驱动强度**：PA28 驱动强度设为 HIGH 后仍不转 → 不是驱动强度问题
3. **定位到 DEBUG 镜像**：注释掉 `UART_send_bytes` 中的镜像功能后 PA28 也能转了 → **根因确认**

**根因**：`uart.c` 中 `UART_send_bytes` 有 `#ifdef DEBUG_INST` 的镜像代码，每次在 PC_INST (PA28) 上发送一个字节时，会在 DEBUG_INST (PA8) 上也同步发送同一个字节。**PA28 和 PA8 都是 GPIOA 端口的引脚（GPIOA bit28 和 bit8），两个引脚同时翻转导致 GPIOA 供电轨上产生电流尖峰，PA28 信号边沿质量下降。**

**为什么 TTL 串口模块能解码但电机不行**：TTL 模块内部有施密特触发器，可以容忍模糊的信号边沿。电机的单片机级接收端对信号完整性要求更高，模糊的边沿导致某些位被误读。

**教训**：
- 不要让同一 GPIO 端口的多个引脚同时以高频翻转
- DEBUG 镜像功能应该做成可配置开关，默认关闭
- 串口助手能解码不等于目标设备能解码 —— TTL 模块有更强的信号容忍能力

---

## 核心经验总结

| # | 教训 | 关键词 |
|---|------|--------|
| 1 | 不要多发不必要的指令（校准、解除保护），每条指令都可能干扰电机内部状态机 | 指令精简 |
| 2 | MSPM0 UART RX FIFO 只有 4 字节深度，发送指令后必须清空应答帧 | FIFO 溢出 |
| 3 | `NVIC_EnableIRQ` 必须手动调用，SysConfig 不会自动生成 | NVIC 中断 |
| 4 | 删除代码时要追踪外部符号引用（`extern`），否则链接报错 | 链接依赖 |
| 5 | 两个 GPIO 引脚同时翻转 → 供电轨噪声 → 信号质量下降 | GPIO 噪声 |
| 6 | 串口助手能解码 ≠ 目标设备能解码，TTL 模块有施密特触发器的容错优势 | 信号完整性 |
| 7 | 更换 UART 通道（PA8 → PA28）是定位问题的有效隔离手段 | 隔离测试 |
| 8 | DEBUG 镜像这类"辅助功能"在生产代码中应默认关闭 | 调试功能隔离 |

---

## 正确的电机控制流程（Emm 固件）

```c
SYSCFG_DL_init();        // 初始化时钟/UART/GPIO
NVIC_EnableIRQ(...);     // 手动使能 NVIC
delay_ms(2000);          // 等电机上电完成

// 使能锁轴
Emm_V5_En_Control(PC_INST, addr, MOTOR_ENABLE, MOTOR_SYNC_NOW);
delay_ms(1500);          // 给电机足够时间完成使能

// 清空应答
while (!DL_UART_isRXFIFOEmpty(PC_INST))
    DL_UART_receiveData(PC_INST);

// 速度指令
Emm_V5_Vel_Control(PC_INST, addr, MOTOR_DIR_CW, 200, 10, MOTOR_SYNC_NOW);

// 停止
Emm_V5_Stop_Now(PC_INST, addr, MOTOR_SYNC_NOW);
```
