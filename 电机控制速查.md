# Emm_V5 电机控制速查

## 核心调用流程

```
1. SYSCFG_DL_init()          → 初始化
2. delay_ms(2000)            → 等电机上电
3. Emm_V5_En_Control(...)    → 使能锁轴
4. Emm_V5_Vel_Control(...)   → 发速度指令（电机开始转）
```

停止：`Emm_V5_Stop_Now(...)`

## 3 个核心函数

### 使能

```c
Emm_V5_En_Control(PC_INST, addr, MOTOR_ENABLE, MOTOR_SYNC_NOW);
//                  ↑       ↑        ↑              ↑
//                UART实例   地址    1=使能/0=释放   0=立即执行
```

### 速度模式

```c
Emm_V5_Vel_Control(PC_INST, addr, dir, vel, acc, snF);
//                    ↑       ↑     ↑    ↑    ↑    ↑
//                    │       │     │    │    │    └─ snF: 多机同步标志
//                    │       │     │    │    │         0 = 立即执行，马上转
//                    │       │     │    │    │         1 = 缓存等待，收到触发指令才转
//                    │       │     │    │    │
//                    │       │     │    │    └─ acc: 加速度档位
//                    │       │     │    │          0 ~ 255，值越大加速越快
//                    │       │     │    │          0 = 直接达到目标速度（无加减速）
//                    │       │     │    │
//                    │       │     │    └─ vel: 目标转速
//                    │       │     │           0 ~ 5000 RPM（Emm 固件）
//                    │       │     │           电机实际最高转速取决于供电电压
//                    │       │     │
//                    │       │     └─ dir: 旋转方向
//                    │       │            0 = CW  顺时针（正转）
//                    │       │            1 = CCW 逆时针（反转）
//                    │       │
//                    │       └─ addr: 电机地址
//                    │                出厂默认 1，范围 1 ~ 255
//                    │                0 为广播地址（所有电机同时响应）
//                    │
//                    └─ PC_INST: UART 外设实例
//                                当前项目用 UART0 (PA28 TX / PA31 RX)
```

**示例：**

```c
/* 地址1的电机以 200 RPM 正转，加速度档位 10，立即执行 */
Emm_V5_Vel_Control(PC_INST, 1, MOTOR_DIR_CW, 200, 10, MOTOR_SYNC_NOW);

/* 地址2的电机以 500 RPM 反转，无加减速，缓存等待同步触发 */
Emm_V5_Vel_Control(PC_INST, 2, MOTOR_DIR_CCW, 500, 0, 1);
```

### 停止

```c
Emm_V5_Stop_Now(PC_INST, addr, MOTOR_SYNC_NOW);
```

### 位置模式

```c
Emm_V5_Pos_Control(PC_INST, addr, dir, vel, acc, clk, raF, snF);
//                     ↑       ↑     ↑    ↑    ↑    ↑     ↑    ↑
//                     │       │     │    │    │    │     │    └─ snF: 同步标志
//                     │       │     │    │    │    │     │         0 = 立即执行
//                     │       │     │    │    │    │     │         1 = 缓存等待同步触发
//                     │       │     │    │    │    │     │
//                     │       │     │    │    │    │     └─ raF: 运动模式
//                     │       │     │    │    │    │            0 = 相对运动
//                     │       │     │    │    │    │                以「上一目标位置」为基准
//                     │       │     │    │    │    │                走 clk 个脉冲后停下
//                     │       │     │    │    │    │            1 = 绝对位置
//                     │       │     │    │    │    │                以「零点」为基准
//                     │       │     │    │    │    │                走到 clk 位置后停下
//                     │       │     │    │    │    │
//                     │       │     │    │    │    └─ clk: 脉冲数
//                     │       │     │    │    │           默认 16 细分: 3200 脉冲 = 1 圈(360°)
//                     │       │     │    │    │           1 脉冲 = 1.8° / 16 = 0.1125°
//                     │       │     │    │    │           换算: 脉冲数 = 角度 × 3200 / 360
//                     │       │     │    │    │           范围: 0 ~ 2³²-1 (约 134 万圈)
//                     │       │     │    │    │
//                     │       │     │    │    └─ acc: 加速度档位
//                     │       │     │    │          0 ~ 255，值越大加速越快
//                     │       │     │    │          0 = 直接达到目标速度
//                     │       │     │    │
//                     │       │     │    └─ vel: 运行过程中的最高转速
//                     │       │     │           0 ~ 5000 RPM
//                     │       │     │           转速越高到达目标位置越快
//                     │       │     │
//                     │       │     └─ dir: 旋转方向
//                     │       │            0 = CW 顺时针
//                     │       │            1 = CCW 逆时针
//                     │       │            相对运动时决定往哪边转
//                     │       │            绝对运动时忽略此参数
//                     │       │
//                     │       └─ addr: 电机地址 (默认 1)
//                     │
//                     └─ PC_INST: UART 外设实例 (UART0)
```

**角度 ↔ 脉冲换算：**

```
脉冲数 = 角度 × 3200 / 360
角度   = 脉冲数 × 360 / 3200

常用值:
   90° →  800 脉冲
  180° → 1600 脉冲
  360° → 3200 脉冲 (1圈)
  720° → 6400 脉冲 (2圈)
```

**示例：**

```c
/* 相对运动：正转 90° (800 脉冲)，转速 100 RPM */
Emm_V5_Pos_Control(PC_INST, 1, MOTOR_DIR_CW, 100, 10, 800, 0, MOTOR_SYNC_NOW);
/* 到达后电机会自动锁在目标位置不动 */

/* 相对运动：反转半圈 (1600 脉冲) */
Emm_V5_Pos_Control(PC_INST, 1, MOTOR_DIR_CCW, 200, 10, 1600, 0, MOTOR_SYNC_NOW);

/* 绝对位置：走到零点 (脉冲数=0)，即回到初始校准位置 */
Emm_V5_Pos_Control(PC_INST, 1, MOTOR_DIR_CW, 200, 10, 0, 1, MOTOR_SYNC_NOW);

/* 绝对位置：走到 180° 位置 (1600 脉冲) */
Emm_V5_Pos_Control(PC_INST, 1, MOTOR_DIR_CW, 200, 10, 1600, 1, MOTOR_SYNC_NOW);
```

**速度模式 vs 位置模式：**

| | 速度模式 | 位置模式 |
|---|---------|---------|
| 行为 | 持续旋转，不停 | 走到目标位置后**自动锁住** |
| 适合 | 传送带、风扇、搅拌 | 云台角度、机械臂关节、定位 |
| 停住方式 | 需手动发停止指令 | 到达后自动停，PID 锁位置 |
| 闭环维持 | 恒转速 | 恒位置（到达后） |

**到达判断：**

位置模式到达目标后会返回 `9F` 应答。可通过读取状态标志位判断是否到位：

```
读取电机状态: 01 3A 6B  →  返回的 bit1 (Prf_TF) = 1 表示已到位
```

**回到固定点的完整流程：**

```c
/* Step1: 校准零点 — 把当前位置设为绝对零位 */
Emm_V5_Reset_CurPos_To_Zero(PC_INST, 1);  // 命令: 01 0A 6D 6B
delay_ms(100);

/* Step2: 使能 */
Emm_V5_En_Control(PC_INST, 1, MOTOR_ENABLE, MOTOR_SYNC_NOW);
delay_ms(1500);

/* Step3: 走到绝对位置 0（回到零点） */
Emm_V5_Pos_Control(PC_INST, 1, MOTOR_DIR_CW, 200, 10, 0, 1, MOTOR_SYNC_NOW);
/* 电机以 200 RPM 走最短路径回到零点，到达后自动锁住 */
```

## 关键宏（Emm_V5.h）

| 宏 | 值 | 含义 |
|----|-----|------|
| `MOTOR_DIR_CW` | 0 | 正转 |
| `MOTOR_DIR_CCW` | 1 | 反转 |
| `MOTOR_ENABLE` | 1 | 使能锁轴 |
| `MOTOR_DISABLE` | 0 | 释放松轴 |
| `MOTOR_SYNC_NOW` | 0 | 立即执行 |
| `MOTOR_ADDR_DEFAULT` | 1 | 出厂地址 |

## 注意事项

- 通信波特率 115200，每条指令末尾固定 `0x6B` 校验
- `NVIC_EnableIRQ(UART0_INT_IRQn)` 必须手动调用
- 每次发指令后清空 RX FIFO 防止溢出
- 指令间延时至少 100ms
- 电机需要独立 12V/24V 供电
- **不要**用 PA28 + PA8 同时发送（DEBUG 镜像）
